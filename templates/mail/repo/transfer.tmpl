<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<title>{{.Subject}}</title>
</head>

{{$url := HTMLFormat "<a href='%[1]s'>%[2]s</a>" .Link .Repo}}
<body>
	<p>{{.Subject}}.
		{{.locale.Tr "To accept or reject it, visit %s or just ignore it." $url}}
	</p>
	<div style="font-size:small; color:#666;">
		<p>
			---
			<br>
			<a href="{{.Link}}">{{.locale.Tr "View it on %s" AppName}}</a>.
		</p>
	</div>
</body>
</html>
